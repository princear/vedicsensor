import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ScheduleTest = () => {
  return (
    <View>
      <Text>ScheduleTest</Text>
    </View>
  );
};

export default ScheduleTest;

const styles = StyleSheet.create({});
